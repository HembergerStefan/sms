import React from 'react';

const Main = () => {
    return (
        <div style={{height: '100%', width: '100%', background: 'var(--nl-clr-1)', gridArea: 'main'}}>
            Profile
        </div>
    );
};

export default Main;